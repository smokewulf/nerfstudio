data:\n  type: "image"\n  images_path: "/root/testing/nerfstudio/images"  # Replace with the actual path\n  camera_poses: "/root/testing/nerfstudio/camera_poses.json"  # Replace with the path to camera parameters\n\nmodel:\n  type: "NeRF"\n  num_samples: 64  # Number of samples per ray for training\n  learning_rate: 0.001  # Learning rate for the optimizer\n  num_epochs: 1000  # Total number of epochs for training\n\ntraining:\n  batch_size: 16\n  log_interval: 50  # Frequency of logging\n  save_checkpoints: True  # Whether to save checkpoints or not
